for ele in range(1, 11):
    print("Serving chai to Token #{ele}")

range(stIdx, endIdx) # endIdx is not inclusive

enumarate() - 
zip(*iterables) - Iterates over several iterables in parallel, producing tuples with an item from each one.

If the else is in the for level, for example:

for ...
    if ...

else

The else block will execute only if the loop doesn't break, if it does - it won't execute.

-------------

Walrus operator (:=) - Assignment + Expression

if (remainder := 15 % 3):
    print(...)